# تقرير ربط قاعدة البيانات الجديدة - موقع shahad
**التاريخ:** 18 أكتوبر 2025  
**الهدف:** إعادة ربط الموقع بقاعدة بيانات جديدة نظيفة باسم `shahad_clean_db`

---

## 📋 ملخص التعديلات

تم إزالة جميع الملفات والأكواد القديمة المرتبطة بقاعدة البيانات السابقة، وإنشاء بيئة نظيفة جديدة تتضمن:

### ✅ الملفات المُنشأة/المُحدثة:
1. **config/database.php** - ملف إعدادات قاعدة البيانات (اسم القاعدة: `shahad_clean_db`)
2. **setup_database.php** - سكربت إنشاء القاعدة والجداول (مع دعم reset)
3. **test_connection.php** - سكربت فحص الاتصال والجداول
4. **js/api.js** - واجهة JavaScript للتواصل مع PHP APIs
5. **api/auth.php** - واجهة برمجية للمصادقة (تم تصحيح المسار)
6. **api/requests.php** - واجهة برمجية للطلبات (المسار صحيح)

### ❌ الملفات المحذوفة:
- config/database_local.php (كان يشير لقاعدة قديمة)
- js/database.js (تم استبداله بـ js/api.js)
- جميع المراجع للقواعد القديمة (website_db، shahad_db)

---

## 🚀 خطوات التشغيل (من الصفر)

### الخطوة 1️⃣: تحضير البيئة
```powershell
# تأكد من وجود XAMPP مُثبت في:
C:\xampp

# تأكد من وجود ملفات المشروع في:
C:\xampp\htdocs\sahahd.HTML
```

**ملاحظة:** إذا كانت ملفات المشروع في مكان آخر (مثل Downloads)، انسخها إلى المسار أعلاه:
```powershell
# انسخ المجلد بالكامل
Copy-Item "C:\Users\sahaa\Downloads\shahad alshahri.HTML\shahad" -Destination "C:\xampp\htdocs\sahahd.HTML" -Recurse -Force
```

---

### الخطوة 2️⃣: تشغيل XAMPP
1. افتح **XAMPP Control Panel** من:
   ```
   C:\xampp\xampp-control.exe
   ```

2. شغّل الخدمتين:
   - **Apache** (اضغط Start)
   - **MySQL** (اضغط Start)

3. تأكد من ظهور اللون الأخضر بجانبهما مع أرقام المنافذ:
   - Apache: Port 80 (أو 8080)
   - MySQL: Port 3306

**حل مشاكل شائعة:**
- إذا فشل Apache: غيّر المنفذ إلى 8080 من Config > httpd.conf
- إذا فشل MySQL: أغلق أي برنامج يستخدم Port 3306 أو شغّل XAMPP كـ Administrator

---

### الخطوة 3️⃣: إنشاء قاعدة البيانات والجداول

افتح المتصفح (Chrome، Edge، Firefox) واكتب في **شريط العنوان** (ليس مربع البحث):

```
http://localhost/sahahd.HTML/setup_database.php?reset=1
```

**ماذا يفعل هذا الرابط؟**
- `reset=1` يحذف قاعدة البيانات القديمة إن وجدت ويعيد إنشاءها من الصفر
- ينشئ القاعدة باسم: `shahad_clean_db`
- ينشئ 5 جداول: users, requests, request_tracking, user_sessions, request_ratings
- يضيف مستخدم إداري افتراضي:
  - **اسم المستخدم:** admin
  - **كلمة المرور:** admin123

**النتيجة المتوقعة:**
```
تم حذف قاعدة البيانات القديمة: shahad_clean_db
تم التأكد من وجود قاعدة البيانات: shahad_clean_db
تم إنشاء جدول المستخدمين بنجاح
تم إنشاء جدول الطلبات بنجاح
تم إنشاء جدول تتبع الطلبات بنجاح
تم إنشاء جدول الجلسات بنجاح
تم إنشاء جدول تقييمات الطلبات بنجاح
تم إنشاء حساب المدير الافتراضي
اسم المستخدم: admin
كلمة المرور: admin123

تم إعداد قاعدة البيانات بنجاح!
```

**ملاحظة:** إذا لم يظهر `reset=1` في الرابط، لن يتم حذف القاعدة القديمة.

---

### الخطوة 4️⃣: فحص الاتصال والجداول

افتح الرابط التالي للتأكد من نجاح الإعداد:

```
http://localhost/sahahd.HTML/test_connection.php
```

**النتيجة المتوقعة:**
```
فحص الاتصال بقاعدة البيانات

الاتصال ناجح ✅

جدول users موجود ✔
جدول requests موجود ✔
جدول request_tracking موجود ✔
جدول user_sessions موجود ✔
جدول request_ratings موجود ✔

إذا كان هناك جداول مفقودة شغل ملف setup_database.php.
```

---

### الخطوة 5️⃣: التحقق من القاعدة عبر phpMyAdmin (اختياري)

افتح phpMyAdmin:
```
http://localhost/phpmyadmin
```

في القائمة اليسرى، ابحث عن قاعدة البيانات: **shahad_clean_db**

اضغط عليها وتأكد من وجود الجداول الخمسة:
1. users
2. requests
3. request_tracking
4. user_sessions
5. request_ratings

يمكنك عرض بيانات جدول `users` للتأكد من وجود المستخدم الإداري `admin`.

---

### الخطوة 6️⃣: تجربة الموقع

#### 🔐 صفحة تسجيل الدخول:
```
http://localhost/sahahd.HTML/login.html
```
- سجّل دخول بالبيانات:
  - **اسم المستخدم:** admin
  - **كلمة المرور:** admin123

#### 📝 صفحة تقديم طلب:
```
http://localhost/sahahd.HTML/request.html
```
- أنشئ طلب جديد لتجربة النظام

#### 📊 صفحة تتبع الطلبات:
```
http://localhost/sahahd.HTML/track.html
```
أو:
```
http://localhost/sahahd.HTML/track_new.html
```
- عرض جميع طلباتك مع تفاصيلها

#### 👨‍💼 لوحة الإدارة:
```
http://localhost/sahahd.HTML/admin.html
```
- إدارة جميع الطلبات وتحديث حالاتها (للمدير فقط)

#### 📈 صفحة الإحصائيات:
```
http://localhost/sahahd.HTML/dashboard.html
```
- عرض إحصائيات النظام

---

## 🛠️ التفاصيل التقنية

### بنية قاعدة البيانات

#### 1. جدول المستخدمين (users)
```sql
id               INT (مفتاح رئيسي)
username         VARCHAR(50) (فريد)
email            VARCHAR(100) (فريد)
password         VARCHAR(255) (مشفر)
full_name        VARCHAR(100)
role             ENUM('admin', 'user')
created_at       TIMESTAMP
updated_at       TIMESTAMP
```

#### 2. جدول الطلبات (requests)
```sql
id               INT (مفتاح رئيسي)
user_id          INT (مفتاح خارجي → users)
request_type     VARCHAR(100)
subject          VARCHAR(200)
description      TEXT
status           ENUM('pending', 'in_progress', 'completed', 'rejected')
priority         ENUM('low', 'medium', 'high', 'urgent')
attachments      TEXT
admin_notes      TEXT
created_at       TIMESTAMP
updated_at       TIMESTAMP
```

#### 3. جدول تتبع الطلبات (request_tracking)
```sql
id               INT (مفتاح رئيسي)
request_id       INT (مفتاح خارجي → requests)
status_change    VARCHAR(100)
notes            TEXT
changed_by       INT (مفتاح خارجي → users)
created_at       TIMESTAMP
```

#### 4. جدول الجلسات (user_sessions)
```sql
id               INT (مفتاح رئيسي)
user_id          INT (مفتاح خارجي → users)
session_token    VARCHAR(255) (فريد)
ip_address       VARCHAR(45)
user_agent       TEXT
expires_at       TIMESTAMP
created_at       TIMESTAMP
```

#### 5. جدول تقييمات الطلبات (request_ratings)
```sql
id               INT (مفتاح رئيسي)
request_id       INT (مفتاح خارجي → requests)
user_id          INT (مفتاح خارجي → users)
quality_rate     INT
speed_rate       INT
created_at       TIMESTAMP
```

---

## 🔧 إعدادات قاعدة البيانات (config/database.php)

```php
<?php
define('DB_HOST', 'localhost');          // عنوان الخادم
define('DB_NAME', 'shahad_clean_db');   // اسم قاعدة البيانات (الجديدة)
define('DB_USER', 'root');               // اسم المستخدم
define('DB_PASS', '');                   // كلمة المرور (فارغة في XAMPP)
define('DB_CHARSET', 'utf8mb4');         // ترميز النصوص
```

**لتغيير اسم القاعدة:**
1. عدّل قيمة `DB_NAME` في الملف أعلاه
2. شغّل `setup_database.php?reset=1` لإنشاء القاعدة الجديدة

---

## 📡 واجهات API المتاحة

### 1. واجهة المصادقة (api/auth.php)

**الإجراءات المتاحة:**
- `login` - تسجيل الدخول
- `register` - تسجيل مستخدم جديد
- `validate` - التحقق من صحة الجلسة
- `logout` - تسجيل الخروج

**مثال استدعاء:**
```javascript
const result = await dbAPI.login('admin', 'admin123');
if (result.success) {
    console.log('تم تسجيل الدخول:', result.user);
}
```

### 2. واجهة الطلبات (api/requests.php)

**الإجراءات المتاحة:**
- `create` - إنشاء طلب جديد
- `get_all` - جلب جميع الطلبات (للإدارة)
- `get_user_requests` - جلب طلبات المستخدم الحالي
- `update_status` - تحديث حالة الطلب
- `get_details` - جلب تفاصيل طلب محدد

**مثال استدعاء:**
```javascript
const result = await dbAPI.createRequest({
    user_id: 1,
    request_type: 'طلب خدمة',
    subject: 'موضوع الطلب',
    description: 'وصف تفصيلي',
    priority: 'medium'
});
```

---

## 🐛 حل المشاكل الشائعة

### مشكلة 1: ظهور 404 Not Found
**السبب:** الملفات ليست في المسار الصحيح  
**الحل:**
- تأكد أن الملفات في: `C:\xampp\htdocs\sahahd.HTML`
- تأكد أن اسم المجلد بالضبط `sahahd.HTML` (حساس لحالة الأحرف)
- جرّب تغيير اسم المجلد إلى `sahahd` فقط (بدون .HTML)

### مشكلة 2: ظهور كود PHP بدلاً من التنفيذ
**السبب:** Apache غير مشغّل  
**الحل:**
- شغّل Apache من XAMPP Control Panel
- تأكد من وجود اللون الأخضر بجانب Apache

### مشكلة 3: خطأ في الاتصال بقاعدة البيانات
**السبب:** MySQL غير مشغّل أو إعدادات خاطئة  
**الحل:**
- شغّل MySQL من XAMPP Control Panel
- افتح `config/database.php` وتأكد من:
  - `DB_HOST` = 'localhost'
  - `DB_USER` = 'root'
  - `DB_PASS` = '' (فارغ)

### مشكلة 4: dbAPI is not defined
**السبب:** ملف `js/api.js` غير محمّل  
**الحل:**
- تأكد من وجود السطر التالي في كل صفحة HTML قبل أي سكربت يستخدم dbAPI:
```html
<script src="js/api.js"></script>
```

### مشكلة 5: الجداول غير موجودة
**السبب:** لم يتم تشغيل `setup_database.php`  
**الحل:**
```
http://localhost/sahahd.HTML/setup_database.php?reset=1
```

---

## ✅ قائمة التحقق النهائية

- [ ] XAMPP مُثبت ويعمل (Apache + MySQL)
- [ ] ملفات المشروع في: `C:\xampp\htdocs\sahahd.HTML`
- [ ] تم تشغيل `setup_database.php?reset=1` بنجاح
- [ ] تم فحص الاتصال عبر `test_connection.php` (5 جداول موجودة)
- [ ] تم اختبار تسجيل الدخول بـ admin/admin123
- [ ] جميع الصفحات تعمل بدون أخطاء في Console

---

## 📞 دعم إضافي

إذا واجهت أي مشكلة:
1. افتح **Developer Console** في المتصفح (اضغط F12)
2. تحقق من تبويب **Console** لرؤية الأخطاء
3. تحقق من تبويب **Network** لرؤية طلبات API الفاشلة
4. شارك رسائل الأخطاء للحصول على مساعدة محددة

---

**انتهى التقرير** ✨  
الموقع الآن جاهز للعمل مع قاعدة بيانات نظيفة جديدة!
